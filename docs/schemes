<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Intuition on Schemes - Ilan Ehrlich</title>

  <!-- MathJax -->
  <script>
    window.MathJax = {
      tex: { inlineMath: [['$', '$'], ['\\(', '\\)']], displayMath: [['$$','$$']] }
    };
  </script>
  <script src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>

  <link rel="stylesheet" href="style.css">
  <style>
    body { background-color:#9BF2FF; color:black; font-family: "Morandi", Arial, sans-serif; padding:20px; max-width:900px; margin:auto;}
    h1 { font-size:2.5em; margin-top:2em; }
    h2 { font-size:2em; margin-top:1.8em; }
    h3 { font-size:1.6em; margin-top:1.5em; }
    h4 { font-size:1.3em; margin-top:1.2em; }
    .theorem, .definition, .example, .remark, .notation { border-left:3px solid black; padding-left:10px; margin:15px 0; }
    .theorem-title, .definition-title, .example-title, .remark-title, .notation-title { font-weight:bold; }
    p { line-height:1.6em; }
    pre { background:#e0f7ff; padding:10px; border-radius:5px; overflow-x:auto;}
    img { max-width:100%; margin:10px 0; }
  </style>
</head>
<body>

<h1>Intuition on Schemes</h1>
<p><strong>Author:</strong> Ilan Ehrlich</p>
<p><strong>Date:</strong> November 2025</p>

<h2>Starter</h2>

<h3>Goal</h3>
<div class="remark">
  <div class="remark-title">Observation:</div>
  By remark 4.16,
  <p>
  $$
  \frac{\{\text{affine varieties over $K$}\}}{\cong} \longleftrightarrow \frac{\{\text{finitely generated reduced $K$-algebra}\}}{\cong}, \quad
  X \mapsto A(X),\quad X_R \mapsfrom R
  $$
  </p>
</div>

<p>
Given a field $K$, affine varieties correspond to finitely generated reduced $K$-algebras.
</p>

<div class="definition">
  <div class="definition-title">Goal 1:</div>
  Generalize affine varieties and their morphisms to more general ringed spaces so the coordinate ring can be any ring $R$.
</div>

<div class="definition">
  <div class="definition-title">Goal 2:</div>
  Define the category of ringed spaces to match generalized affine varieties.
</div>

<h3>Idea</h3>
<p>
Instead of viewing a variety as the set of points, view it as the set of its irreducible subvarieties. This gives a better perspective on the coordinate ring, since prime ideals correspond to irreducible affine subvarieties.
</p>

<h2>Reformulations of Affine Varieties</h2>

<h3>As Sets</h3>
<p>
Focus on the set
$$
X_\text{sch} := \{ Y : Y \text{ affine irreducible subvariety of } X \}
$$
rather than $X$ itself.
</p>

<h3>As Topological Spaces</h3>
<p>
Define closed subsets of $X_\text{sch}$ as
$$
\{ Y : Y \subseteq Y_0, Y \text{ affine irreducible} \}
$$
for $Y_0$ affine (not necessarily irreducible). This defines a topology.
</p>

<h3>As Ringed Spaces</h3>
<p>
A regular function on an open $U \subseteq X$ is a local rational function:
$$
\forall x\in U\ \exists f,g\in A(X), x\in U_x \subseteq U: \forall y \in U_x, f(y)\neq 0, \varphi(y) = \frac{g(y)}{f(y)}
$$
</p>

<p>
On $U_0 = \{ Y : Y \nsubseteq Y_0, Y \text{ irreducible affine}\}$, a function is a collection $(\varphi_Y)_{Y \in U_0}$.
</p>

<p>
Evaluation of $\varphi$ at $Y$ corresponds to $\varphi_Y \in A(X)_{I(Y)}$, giving rational functions locally on each irreducible affine subvariety.
</p>

<h2>Schemes</h2>

<h3>Construction</h3>

<h4>From Affine Varieties</h4>

<h5>As Ringed Spaces</h5>
<p>
Consider $X_\text{sch}$ as the set of prime ideals of $A(X)$:
$$
X_\text{sch} := \{\mathfrak{p}: \mathfrak{p} \text{ prime ideal of } A(X)\} = \mathrm{Spec} A(X)
$$
</p>

<p>
For a general ring $R$, define closed sets as
$$
V(I) = \{\mathfrak{p}: I \subseteq \mathfrak{p}\}
$$
and the structure sheaf:
$$
\mathcal{O}_{\mathrm{Spec} R}(U) = \{ (\varphi_\mathfrak{p})_{\mathfrak{p} \in U}: \forall \mathfrak{p} \in U, \exists f,g \in R, \forall \mathfrak{q} \in U_\mathfrak{p}, f \notin \mathfrak{q}, \varphi_\mathfrak{q} = g/f \in R_\mathfrak{q} \}.
$$
</p>

<div class="definition">
  <div class="definition-title">Definition:</div>
  An <strong>affine scheme</strong> is a ringed space of the form $\mathrm{Spec} R$ with the above sheaf.
</div>

<h5>Being over a Field</h5>
<p>
If $X$ is an affine variety over $K$, then $A(X)$ is a $K$-algebra, giving a scheme morphism $\mathrm{Spec} A(X) \to \mathrm{Spec} K$.
</p>

<h4>From Arbitrary Varieties</h4>
<p>Generalizations include finite covers, being reduced, separated, etc.</p>

<div class="definition">
  <div class="definition-title">General Scheme:</div>
  A scheme obtained from these constructions satisfying the usual axioms of schemes.
</div>

<h2>Ringed Space Morphisms</h2>

<p>Details on morphisms of ringed spaces and further properties of schemes.</p>

<!-- Example image -->
<img src="example.jpeg" alt="Example diagram" />

</body>
</html>
